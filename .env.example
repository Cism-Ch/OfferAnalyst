# ============================================================================
# AI Models & APIs
# ============================================================================
# Deprecated: Google Gemini API Key (use OPENROUTER_API_KEY instead)
GEMINI_API_KEY=-

# OpenRouter API Key - Primary AI provider for all LLM operations
# Get your key from: https://openrouter.ai/keys
# Used by: analyze.ts, fetch.ts, organize.ts server actions
OPENROUTER_API_KEY=sk-or-v1-


# ============================================================================
# Database Configuration
# ============================================================================
# MongoDB Connection URI (for Prisma)
# 
# LOCAL DEVELOPMENT (Docker or local MongoDB):
# DATABASE_URL=mongodb://localhost:27017/offeranalyst
#
# MONGODB ATLAS (Cloud - Recommended for Production):
# 1. Create account at https://www.mongodb.com/cloud/atlas
# 2. Create a cluster (M0 Free Tier for development)
# 3. Add database user in Security > Database Access
# 4. Whitelist IP in Security > Network Access (0.0.0.0/0 for Vercel)
# 5. Get connection string from Connect > Drivers
# Format: mongodb+srv://username:password@cluster.mongodb.net/offeranalyst?retryWrites=true&w=majority
# 
# IMPORTANT: URL-encode special characters in password (e.g., @ becomes %40)
# See docs/MONGODB_SETUP.md for detailed setup instructions
DATABASE_URL=mongodb://localhost:27017/offeranalyst

# MongoDB Connection URI (legacy, kept for backwards compatibility)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
# Simplified URI to avoid parsing issues with special characters in shell/prisma
MONGODB_URI=

# ============================================================================
# File Storage & Search
# ============================================================================
# Tigris S3-compatible Object Storage Configuration
# Get credentials from: https://www.tigrisdata.com/

# S3 bucket name for storing uploaded offers and files
TIGRIS_BUCKET_NAME=

# Tigris storage endpoint (default: t3.storage.dev)
TIGRIS_STORAGE_ENDPOINT=https://t3.storage.dev

# Tigris secret access key for authentication
TIGRIS_STORAGE_SECRET_ACCESS_KEY=

# Tigris access key ID for authentication
TIGRIS_STORAGE_ACCESS_KEY_ID=

# ============================================================================
# Feature Flags
# ============================================================================
# Enable Tigris-powered semantic search for offers
ENABLE_TIGRIS_SEARCH=false

# Enable event tracking and analytics
ENABLE_EVENTS=false

# ============================================================================
# Authentication & Session (Better-Auth)
# ============================================================================
# Secret key for signing JWT tokens and session cookies
# Generate a random string using: openssl rand -base64 32
# MUST be at least 32 characters long for production
# Keep this secret and never commit to version control
BETTER_AUTH_SECRET=

# Base URL for authentication flows
# LOCAL: http://localhost:3000
# PRODUCTION: https://your-domain.vercel.app (or your custom domain)
# Must match NEXT_PUBLIC_APP_URL
BETTER_AUTH_URL=http://localhost:3000

# Public app URL - exposed to browser for OAuth redirects and client-side features
# Must be accessible from browser (use actual domain in production)
# LOCAL: http://localhost:3000
# PRODUCTION: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# OAuth Providers (Optional - for social login)
# ============================================================================

# Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: {BETTER_AUTH_URL}/api/auth/callback/google
# 3. Copy Client ID and Client Secret
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth Configuration  
# Get credentials from: https://github.com/settings/applications/new
# 1. Register new OAuth App
# 2. Set callback URL: {BETTER_AUTH_URL}/api/auth/callback/github
# 3. Copy Client ID and generate Client Secret
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ============================================================================
# Email Service (Optional - for verification emails)
# ============================================================================
# Email Service (Resend)
# Get API key from: https://resend.com/api-keys
# Used for: Email verification, password reset emails
# Leave empty to disable email features (requireEmailVerification will be false)
RESEND_API_KEY=